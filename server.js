const joi = require('joi');
const shortId = require('shortid');
// const passwordSchema = joi.string().min(3).max(6).alphanum();
// console.log(passwordSchema.validate('23erlkj'));
console.log(shortId.generate());